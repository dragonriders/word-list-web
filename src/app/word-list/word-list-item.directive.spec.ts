import { WordListItemDirective } from './word-list-item.directive';

describe('WordListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new WordListItemDirective();
    expect(directive).toBeTruthy();
  });
});
